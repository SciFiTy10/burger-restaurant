import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#A52A2A",
    },
    secondary: {
      main: "#681A1A",
    },
    text: {
      secondary: "#fff",
    },
  },
});

export default theme;
